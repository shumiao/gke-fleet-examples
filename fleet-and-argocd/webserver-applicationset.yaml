apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: webserver-applicationset
spec:
  generators:
  - clusters:
      selector: 
        matchLabels:
          argocd.argoproj.io/secret-type: cluster
          createdBy: argocd-fleet-syncer
          fleetScope: frontend
  template:
    metadata:
      name: '{{name}}-webserver' 
    spec:
      project: default
      source:
        repoURL: https://github.com/shumiao/gke-fleet-examples
        targetRevision: HEAD
        path: fleet-and-argocd/webserver-application/manifests
      destination:
        server: '{{server}}'
        namespace: webserver
